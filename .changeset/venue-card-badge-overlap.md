---
'@crawl/mobile': patch
---

Fix two spacing defects reported on the v1.1.0 TestFlight build: venue card badges no longer overlap the placeholder icon on image-less venues (they render inline above the venue name instead of overlaying the short hero), and the Explore filter chip row now has breathing room above the map.
