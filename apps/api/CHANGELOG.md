# @crawl/api

## 1.0.9

### Patch Changes

- 89a041d: #126: added `public.cast_vote(p_venue_id)`, a `SECURITY DEFINER` Postgres RPC that enforces the 3-vote/day cap (global per user, #62/#134) and per-venue dedup atomically, inside the transaction — closing the gap where the equivalent check in `apps/api/src/services/vote.service.ts` only protects the Fastify path, which isn't deployed for this beta and is trivially bypassable via PostgREST's auto-generated `POST /rest/v1/votes` using the client-bundled anon key. Also introduced `public.vote_day(at, tz)`, a canonical SQL port of `packages/shared-types/src/voteDay.ts`'s `voteDayFor` (04:00 city-local boundary, `America/New_York` default), and re-pointed 0006's `recalculate_hotspot_scores()` at it via `CREATE OR REPLACE` (0006 itself is left immutable) — fixing a day-boundary mismatch where the trending list's `daily_count` and the vote budget disagreed for a 4-hour window every night. `cast_vote` also now owns `venues.vote_count`, deliberately left unmaintained by 0006. Shipped as `apps/api/drizzle/0007_cast_vote_rpc.sql`, recorded in `apps/api/drizzle/meta/_journal.json` and `0007_snapshot.json` via `drizzle-kit generate --custom`, matching the 0005/0006 convention. EXECUTE is revoked from `PUBLIC` and `anon` and granted only to `authenticated` (which covers this beta's anonymous-sign-in users); `auth.uid()` is the sole source of voter identity. Verified against a scratch local Postgres 16 (cap exhaustion, dedup, global-not-per-city scope, `vote_count` increments, the 03:59:59/04:00:00 boundary matching `voteDayFor`/`voteDayResetAt` exactly, concurrent-request cap enforcement via `pg_advisory_xact_lock`, and ACL survival across `CREATE OR REPLACE`) — nothing applied to the live Supabase project (connector unauthorized this session). Adds an opt-in Vitest integration suite (`apps/api/tests/db/cast-vote-rpc.test.ts`, gated on `TEST_DATABASE_URL`, skipped by default) covering cap exhaustion and duplicate-vote rejection against the real migration SQL.
- 0de2363: #196: added `public.get_vote_state()`, a `SECURITY INVOKER` Postgres RPC that returns the caller's own vote state (`{ remainingVotes, maxVotes, votedVenueIds, resetAt }`) for the current vote day — the missing read-path counterpart to #126's `cast_vote` RPC. Fixes the beta's actual failure mode: with `EXPO_PUBLIC_API_URL` unset and Supabase configured, `useVoteState` falls through to device-local mock state while votes are written to Postgres, so a reinstall appears to reset the daily budget while the server still holds the real count, and the next cast is rejected with `NO_VOTES_REMAINING` against a UI claiming votes remain. `get_vote_state` runs `SECURITY INVOKER`, not `SECURITY DEFINER` like `cast_vote` — a read of the caller's own rows needs no elevated privilege, since `0002_rls_policies.sql`'s existing `"votes: read own"` policy already restricts it correctly. Also extracts `public.vote_max_daily_votes()` and `public.vote_reset_at(at, tz)` as shared helpers (both built on 0007's `public.vote_day()`) so the vote cap and the resetAt computation each have exactly one definition, and re-points `cast_vote` at them via `CREATE OR REPLACE` (0007 itself stays immutable) — no behavior change, same 26 existing `cast_vote` tests still pass. EXECUTE is revoked from `PUBLIC` and `anon`, granted only to `authenticated`; `auth.uid()` is the sole identity source, no user-id parameter. Shipped as `apps/api/drizzle/0008_get_vote_state_rpc.sql`, recorded in `apps/api/drizzle/meta/_journal.json` and `0008_snapshot.json` via `drizzle-kit generate --custom`. Verified against a scratch local Postgres 16 (zero-votes-today returns full budget, today's votes reflected in `remainingVotes`/`votedVenueIds`, a previous-vote-day row excluded, `resetAt` parity with `cast_vote` for the same instant, unauthenticated rejected with `AUTH_REQUIRED`, `anon` denied EXECUTE outright) — nothing applied to the live Supabase project (connector unauthorized this session). Extends the existing opt-in `apps/api/tests/db/cast-vote-rpc.test.ts` suite (gated on `TEST_DATABASE_URL`, now exercised in CI against a `postgres:16-alpine` service container as of the prior commit) rather than a sibling file, since a second file DROP/CREATEing the same unnamespaced `public.votes`/`venues`/`users` objects would race it under Vitest's default file-parallel execution. Mobile's read-branch fix (`apps/mobile/src/api/votes.ts`'s `useVoteState`, still gated on `hasApi` only) is out of scope here and reported back to the orchestrator as a follow-up.
- f11533c: #154: fixed `hotspot_score` (and, downstream, `is_trending`) sitting at 0 for every venue on the Supabase-native path, where the hourly node-cron recalculation job never runs because the Fastify API isn't deployed for this beta. Added `apps/api/drizzle/0006_hotspot_score_pg_cron.sql`, a migration that ports the scoring formula from `apps/api/src/jobs/recalculate-scores.ts` into a `SECURITY DEFINER` Postgres function (`public.recalculate_hotspot_scores()`) scheduled hourly via `pg_cron`, and seeds a non-zero baseline immediately on apply so Global Rankings and Explore aren't a flat, arbitrarily-ordered list of zeros on day one. `vote_count` is left untouched — that column belongs to #126's `cast_vote` RPC. The function pins `search_path = public, pg_temp` and revokes `EXECUTE` from `PUBLIC` (granting it only to the migration/cron-owning role) to close a temp-table-shadowing privilege-escalation path and an unauthenticated-PostgREST-RPC path caught in security review. Nothing has been applied to any live database; `pg_cron` availability on the project's Supabase plan, and the exact role `cron.schedule` runs jobs as, are both unverified.
- 4c96b15: #64: fixed the daily vote budget resetting at UTC midnight (7-8pm Eastern, mid-evening — exactly when voting matters most) instead of at a sensible nightlife-day boundary. Added a canonical `voteDayFor`/`voteDayResetAt` helper to `@crawl/shared-types` (`packages/shared-types/src/voteDay.ts`) so the API, mobile mock store, and mobile countdown share one definition instead of drifting independently. The vote day now rolls over at 04:00 in the relevant city's timezone (`public.cities.timezone`, all `America/New_York` today) rather than the raw UTC calendar date, using a real `Intl.DateTimeFormat`-based zoned-time conversion (verified across the 2026 DST transitions) instead of a fixed UTC offset. `apps/api/src/services/vote.service.ts`'s `today()`, the `VoteRepository.create`/`delete` implementations (which previously stamped `votedAt` from either a second, independent `today()` or Postgres's `CURRENT_DATE` default), and the `reset-votes` cron schedule (now `04:00 America/New_York` instead of `00:00 UTC`) all derive from the shared helper. Mobile now shares the same boundary: `apps/mobile/src/api/voteStorage.ts`'s mock vote-state date key and `apps/mobile/src/hooks/useCountdown.ts`'s reset countdown both derive from the shared helper (falling back to `DEFAULT_VOTE_DAY_TIMEZONE` — mobile doesn't resolve a per-city timezone yet) instead of raw UTC date/local midnight.
- Updated dependencies [4c96b15]
  - @crawl/shared-types@1.0.3

## 1.0.8

### Patch Changes

- 7ebbfd6: Sprint 4 users/venues migration wave (#77, #129, #131, #167 remediation, #156):

  - Removed the dead custom bcrypt `/auth/register`, `/auth/login`, and `/auth/refresh` routes, `AuthService`, and the `UserRepository`/`DrizzleUserRepository` pair. These only ever ran in local in-memory dev mode and were unreachable in production, where Supabase JWT verification via JWKS (`plugins/jwt.ts`) is the only auth path. Dropped `users.password_hash` and the `bcryptjs` dependency accordingly.
  - Added `public.users.device_id` (abuse/dedup signal only, no auth-flow use) and `public.users.role` (`'user' | 'developer'`, `text` with a `CHECK` constraint). Replaced the per-request Supabase-user upsert in `jwt.ts` (which only fired for users hitting an authenticated endpoint) with a `SECURITY DEFINER` trigger on `auth.users` that provisions a `public.users` row for every identity, anonymous or linked, at creation time — plus a backfill for the 14 identities that predated it. Tightened the `users: update own` RLS policy's practical effect by revoking table-wide `UPDATE` from `authenticated` and re-granting it only on non-`role` columns, since a column-level `REVOKE (role)` alone does not narrow a table-wide grant.
  - Reassigned the 27 `South End, Charlotte` venues to `Charlotte` and removed the `South End, Charlotte` `cities` row (Charlotte's existing 8000m radius already covers them). Corrected Sayville's `radius_meters` from 250 (a dropped zero) to 2500, matching Patchogue's radius for a same-scale town. Reassigned or deactivated the remaining out-of-radius venues from #167 based on an actual distance-to-city-center recomputation, rather than the ingest-time snapshot.

## 1.0.7

### Patch Changes

- 1eb5163: Fix the venue sync job silently assigning out-of-radius Google Places results to the city being synced (#167). `locationBias` on `searchText` was always a soft preference, not a hard filter, and nothing validated distance before stamping a place with `cityId`. The sync job now rejects any place farther than `radiusMeters` from the city's geocoded center before it is ever upserted, logging the venue name and distance and recording the rejection in `SyncCityResult.errors` so out-of-bounds counts are visible in a sync run.
- 9a562aa: Bump `node-cron` from `^3.0.3` to `^4.6.0` to clear the `uuid < 11.1.1` buffer-bounds-check advisory (GHSA-w5hq-g745-h8pq), which was pulled in transitively via node-cron's v3 dependency on `uuid@8.3.2`. node-cron v4 is a zero-dependency rewrite, so the advisory is removed entirely rather than patched. Updated the two job files (`reset-votes.ts`, `recalculate-scores.ts`) for v4's typings, which now export `ScheduledTask` as a named type rather than a property of the default import; schedules and timezone behavior (daily vote reset at 00:00 UTC, hourly hotspot score recalculation) are unchanged. Also dropped the now-redundant `@types/node-cron` devDependency since v4 ships its own type declarations.
- 23cec89: Bump `zod` to v4 and `fastify-type-provider-zod` to v7 together (Dependabot #87/#88 must land as one coordinated upgrade since v7 of the provider targets Zod 4 specifically). Tightened UUID validation in Zod 4 (`.uuid()` now enforces RFC 4122 version/variant nibbles) required switching request/response UUID schemas from `.uuid()` to the RFC-format-agnostic `.guid()` to preserve existing validation behavior for non-v4 identifiers.
- Updated dependencies [23cec89]
  - @crawl/shared-types@1.0.2
