# Changelog files are generated by changesets and don't need formatting.
# Excluding them also prevents the prettier-plugin-tailwindcss ESM/CJS
# conflict that occurs when changesets formats apps/mobile/CHANGELOG.md
# using the mobile workspace's prettier config.
**/CHANGELOG.md
